# ==========================================
# Winwin Crawler 3.3 Environment Variables
# ==========================================

# 1. API Keys
# 구글 제미나이 AI 시크릿 키
GEMINI_API_KEY=AIzaSyDcyjVgYktiGvTKLioDM9WhQo3Et5S8dXI

# 텔레그램 봇 토큰 (에러 알림 등)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# 2. Auth Server (인증 서버)
# JWT 토큰 등을 생성/검증할 때 사용하는 비밀 키 (임의의 긴 문자열)
AUTH_SECRET_KEY=replace_with_a_long_random_secret

# 초기 관리자 계정 생성 시 사용할 비밀번호
INITIAL_ADMIN_PASSWORD=replace_with_a_temporary_admin_password

# 3. Profile Encryption
# 로컬에 저장된 프로필(카카오/밴드) 비밀번호를 암호화하기 위한 대칭키 (Fernet 키)
# 생성 방법: 파이썬 프롬프트에서 아래 명령어 실행
# from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())
PROFILE_ENCRYPTION_KEY=replace_with_a_generated_fernet_key
